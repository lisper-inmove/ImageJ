# Google C++ 编码规范总结

## 1. 头文件
- **使用头文件保护宏**：`#pragma once` 或 `#define FILENAME_H_`。
- **避免使用 `#include` 在头文件中包含不必要的依赖**，优先使用前置声明。
- **内联函数**：仅在小于 10 行或性能关键时使用。
- **`#include` 顺序**：相关头文件、C 库、C++ 库、其他库、项目内头文件。

## 2. 作用域
- **命名空间**：使用匿名命名空间或 `static` 实现文件私有化；禁止使用 `using namespace` 在头文件中。
- **全局变量**：尽量避免，必须使用时加 `g_` 前缀。
- **局部变量**：尽量声明在首次使用处，并初始化。
- **静态变量**：避免在头文件中定义静态变量。

## 3. 类
- **构造函数**：避免进行复杂初始化，优先使用初始化列表。
- **显式构造函数**：单参数构造函数应使用 `explicit` 关键字。
- **拷贝/移动构造与赋值**：明确声明 `= default` 或 `= delete`。
- **继承**：优先使用组合；继承时基类析构函数应为虚函数。
- **访问控制**：按 `public`、`protected`、`private` 顺序声明。

## 4. 函数
- **函数长度**：尽量不超过 40 行。
- **参数顺序**：输入参数在前，输出参数在后（输出参数使用指针或引用）。
- **函数重载**：谨慎使用，避免歧义。
- **默认参数**：仅在非虚函数中使用，且避免在虚函数中。

## 5. 命名约定
- **文件名**：全小写，下划线分隔，如 `my_class.cc`。
- **类型名（类/结构体/枚举）**：大驼峰，如 `MyClass`。
- **变量名**：小写，下划线分隔，如 `my_variable`。
- **类成员变量**：末尾加下划线，如 `my_member_`。
- **常量名**：`k` 开头大驼峰，如 `kMyConstant`。
- **函数名**：大驼峰，如 `MyFunction()`。
- **命名空间**：全小写，如 `mynamespace`。
- **宏名**：全大写，下划线分隔，如 `MY_MACRO`。

## 6. 注释
- **文件注释**：每个文件开头应有版权、作者、简要描述。
- **类注释**：描述类的用途与使用方式。
- **函数注释**：描述功能、参数、返回值、异常等。
- **行内注释**：解释复杂逻辑，非显而易见代码。
- **TODO 注释**：`// TODO(username): 待处理事项`。

## 7. 格式
- **行宽**：不超过 80 字符。
- **缩进**：2 个空格，不使用 Tab。
- **大括号**：行末开括号，单独闭括号（K&R 风格）。
- **指针/引用**：`*` 和 `&` 紧贴类型名，如 `int* ptr`。
- **条件/循环**：必须使用大括号，即使只有一行。

## 8. 异常与 RTTI
- **异常**：Google 规范禁止使用异常（C++ 异常）。
- **RTTI**：禁止使用 `dynamic_cast` 和 `typeid`（除非在测试中）。

## 9. 类型转换
- 使用 C++ 风格转换：`static_cast`、`const_cast`、`reinterpret_cast`，避免 C 风格转换。

## 10. 其他
- **智能指针**：优先使用 `std::unique_ptr`，必要时使用 `std::shared_ptr`。
- **禁止使用 `long long` 以外的变长整数（如 `int64_t`）**。
- **禁止使用 `goto`、`setjmp`、`longjmp`**。

---

**参考**：[Google C++ Style Guide](https://google.github.io/styleguide/cppguide.html)